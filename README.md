# Backend - Universal Video Downloader

## Run locally
pip install -r requirements.txt
uvicorn main:app --host 0.0.0.0 --port 8000

## Deploy
Use Render / Railway / Replit

## Important
Install ffmpeg:
apt-get update && apt-get install -y ffmpeg

